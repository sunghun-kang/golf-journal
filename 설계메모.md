# 강'골프 설계메모 (Design Memo)

> 최종 업데이트: 2026-09-20 · 골프앱 **V01.02.19** · 자산앱 **v01.00.33**

개인용 단일 파일 웹앱 모음. 빌드 없음(순수 HTML/CSS/JS), GitHub Pages 호스팅.

## 0. 구성 / 호스팅
- 레포: `sunghun-kang/golf-journal`, 배포 브랜치 **main** (GitHub Pages)
- 개발 브랜치: `claude/naegolf-app-a9p4hz` → 작업 후 main에 반영
- 공개 URL: `https://sunghun-kang.github.io/golf-journal/`
- 런처: `index.html` (강'Story / 랴쟈가족 / 💰 내 자산 카드)
- 앱들:
  - `golf-swing-journal_25.html` — **강'Story** (골프 스윙 일지)
  - `portfolio-web/index.html` — **내 자산** (증권·은행 통합 자산현황)
  - `golf-family.html` — 랴쟈가족
- 캐시 이슈: 브라우저가 HTML을 오래 캐시함 → 강제 최신은 URL에 `?v=NN` 붙이기, 또는 탭 종료 후 재접속.

## 1. 공용 서버(Supabase)
- URL: `https://qnbpavvcpxjhccppiqav.supabase.co`
- publishable key: `sb_publishable_OtaDk4iYFhZJs5GBUz7btw_mOtE5MJP`
- 테이블 `golf_posts` (컬럼: `id` PK, `data` jsonb, `updated_at`)
- upsert: POST + `Prefer: resolution=merge-duplicates`
- 특수 예약 id (일반 글이 아님):
  - `__meetings__` — 기산전자 회의록 목록
  - `__theme_extra__` — 테마 추가 미디어
  - `__asset_port__` — 자산앱 포트폴리오(PORT)
  - `__asset_hist__` — 자산앱 차트 이력(hist)
- 골프앱 `applyServerRows`는 위 특수 id를 전용 처리/무시(`__asset*`는 무시)해 데이터 오염 방지.

## 2. 강'Story (골프앱)
### 데이터 모델 (record)
```
{ id, date:"YYYY-MM-DD", vt, sub, venue, members, title, memo,
  important, pinned, color, bold, media:[{type:'image|video',data:dataURI}],
  updatedAt, createdAt }
```
- `vt` 구분(1단): F 필드 / S 스크린 / A 분석 / L 人生 / E 영어 / C 회사
- `sub` 세부(2단): 어떤 구분이든 입력 가능. 人生은 금융/연예/운동 추천, 나머지는 자유입력. 편집창에서 구분 옆칸.
- id 규칙: `date_제목10자_랜덤4`. 삭제는 tombstone(`{deleted:true}`) 전파.

### 동기화
- Supabase 델타 동기화(`golfSyncAt2` 워터마크), 로드시 pull. `postRows` upsert.
- ⋮ 메뉴: 백업파일 저장/불러오기, 서버 전체받기(resyncAll), **미동기화 확인(checkUnsynced)** = 서버 id ↔ 로컬 비교 후 폰전용 기록 재업로드, 자동백업, 연동설정.
- 미디어가 너무 크면 그 글은 업로드 실패 → 폰 로컬에만 남음(미동기화 확인으로 검출).

### 주요 기능
- 상단 탭(헤더 1줄, 색상): 목록(파랑)/달력(초록)/테마(보라)/회의(주황)
- 목록 뱃지: 타입/세부/구장(venue)/멤버 칩 — 중간 파스텔 배경+색글씨, **고정 높이 22px** 통일, subTint() 색맵.
- 달력(월간), 공휴일/대체공휴일 표시.
- 회의록(mtg*): 정기회의 자동생성, 구글캘린더 가져오기, 병합 동기화.
- 테마(🔍찾기/🤖질문/📊분석): 기간·키워드·구분 필터 → 관련 기록을 Claude API로 분석.
  - 열 때 이전 키워드 자동복원 안 함 + ✕ 지우기 버튼.
  - **themeAsk 기간/구분 인식**: "9월"=올해 그 달(date 필터), 이번달/지난달/최근; 필드·스크린·영어·회사·人生 = vt 타입 필터; 참고한 기록(날짜·구분) 목록 표시.
- 📸 스크린샷(html2canvas 지연로딩 → 공유시트/다운로드), Ctrl+V 사진붙여넣기.
- Claude API: `api.anthropic.com/v1/messages`, model `claude-opus-5`, 헤더 x-api-key(+dangerous-direct-browser-access). 키는 기기 로컬(localStorage `anthropicKey` + IndexedDB), 🔑키 모달로 입력.

## 3. 내 자산 (자산앱)
- 상수: `LS_KEY="portfolio_v1"`, `DATA_VERSION=9`, `HKEY="asset_hist_v2"`
- 기관 소스(차트): `SRC=[내앱(me,자동)/네이버/카카오/토스/KB]` — 내앱만 자동, 나머지 수기.
- EMBEDDED 계좌(관리자 기준): 토스증권, 카카오페이증권, 한국투자(국내/해외), NH투자증권, NH Lab, IBK기업은행, KB국민은행, 코인.
- 서버 동기화: `__asset_port__`(baseV=DATA_VERSION 가드), `__asset_hist__`(날짜별 병합). 자동 push는 **read-merge-write**(saveHist→histPushMerge, 1.5s 디바운스)로 타기기 데이터 보존.
- 차트(총자산 추이): 엑셀형 표(가로=날짜/세로=기관, 천원단위), 내앱 밑 기관별 sub행(meAcc), 매주월 자동칸, 금주 스크롤, SVG 선그래프.
- 내앱 숫자칸 탭 = 그 날짜에 현재 총액+기관별 스냅샷(chartSnapMe). **미래 날짜 기록 차단**.
- +날짜: 보이는 date input(onchange)로 칼럼 추가.
- 날짜/주 계산 **KST(서울) 고정**(thisMonday/isMon UTC+9).
- ☁동기화 수동 버튼(assetSyncNow) + 진단 알림.

## 4. 배포 워크플로
1. 개발 브랜치에서 파일 수정 → 버전 문자열 bump
2. `node -e`로 인라인 스크립트 `new Function()` 검증 (0 errors 확인)
3. 브랜치 커밋/푸시 → main 체크아웃/풀 → 브랜치 파일 체크아웃 → main 커밋/푸시 → 브랜치 복귀
4. GitHub Pages가 main 서빙(1~2분), 캐시는 `?v=` 로 우회

## 5. 알려진 제약
- 원격(클라우드) 환경에서 Supabase 호스트는 네트워크 allowlist에 없어 **직접 조회/쓰기 불가** → 앱은 사용자 브라우저에서 정상 동작.
- 한국 은행/증권 잔액 자동조회 불가(오픈뱅킹/마이데이터 라이선스 필요) → 수기 입력.
- 브라우저 HTML 캐시로 최신 반영 지연 가능.
